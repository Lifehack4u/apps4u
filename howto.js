/**
 * Create a rout for your App
 */
//  Inside the routes directory create a new js file and  call it f.e myapp.js
//  Copy the test.js to your new cretaed file and chenage it according to your App routes.

// Creat a view :
// Create a dir for your app inside the frontend/source/apps. Call it f.e myapp
// Myapp dir must have this folder html, js. And this files: config.json and index.scss
// Your JS files will exist  inside the js folder and your html files will exist  inside the html floder

// The myapp/html/main.ejs file will be the entry point for your html code.
// The myapp/js/inde.js file will be the entry point for your js files
// The myapp/index.scss file will be the entry point for your scss files

// First time :
// Run npm run dev --app_name=myapp for localy deploi=ying your app.
// Run node index.js
// Go to localhost:300/myapp

// Then:
// Every time yo change the frontend/source you have to run npm run dev --app_name=myapp to get changes.

// NODEJS, EXPRESSJS and EJS template